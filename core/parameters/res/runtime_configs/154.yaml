yield_with_id_host_fns: { old: false, new: true }
# yield_create_with_id charges this base instead of wasm_yield_create_base.
# Accounts for the two extra trie writes (YieldIdToDataId / DataIdToYieldId
# mapping) compared to plain yield_create.
# TODO: replace placeholder with an estimated value before stabilization.
wasm_yield_create_with_id_base: { old: 300_000_000_000_000, new: 290_000_000_000 }
