image: "rust:latest"

cache:
    key: "${CI_JOB_NAME}"
    paths:
        - ./target/
        - ./.cargo/

test:cargo:
    stage: test
    script:
    - ./init.sh
    - ./build.sh
    - rustc --version && cargo --version
    - cargo test --all --verbose

