# Runtime & transaction/receipt execution

> Protocol version: 87 (stable) · Release: 2.14.0-rc.1 · Derived from commit: 233252e · Generated: 2026-09-14
> Primary crates/files: `runtime/runtime/src/lib.rs`, `runtime/runtime/src/actions.rs`, `runtime/runtime/src/verifier.rs`, `runtime/runtime/src/config.rs`, `runtime/runtime/src/action_validation.rs`, `runtime/runtime/src/access_keys.rs`, `runtime/runtime/src/function_call.rs`, `runtime/runtime/src/global_contracts.rs`, `runtime/runtime/src/deterministic_account_id.rs`, `runtime/runtime/src/universal_account_id.rs`, `runtime/runtime/src/receipt_manager.rs`, `core/primitives-core/src/version.rs`, `core/parameters/res/runtime_configs/87.yaml`

## Role

The runtime is the state-transition function of a single shard. `Runtime::apply` takes a chunk (transactions + incoming receipts) plus the pre-state trie and produces a new state root, outgoing receipts, execution outcomes, validator proposals, and congestion/bandwidth metadata (`runtime/runtime/src/lib.rs:1860` — `Runtime::apply`, `runtime/runtime/src/lib.rs:388` — `ApplyResult`). It validates and converts transactions into receipts, executes action receipts one action at a time, accounts gas/deposits/refunds, and commits or rolls back trie state per receipt. It sits downstream of chunk/block production and epoch management (which supplies the validator accounts update and the runtime config), invokes the [contract VM](contract-vm.md) for `FunctionCall`, routes outgoing receipts through [cross-shard congestion control](cross-shard-congestion.md), and reads/writes the shard via [state storage](state-storage.md). Fees feed [economics](economics.md); stake proposals feed [epoch, validators & staking](epoch-validators-staking.md); receipt/action wire types are documented in [data structures & serialization](data-structures-serialization.md) and account/key semantics in [accounts & keys](accounts-keys.md).

## Key data structures

- **`ApplyState`** — `runtime/runtime/src/lib.rs:167` — inputs constant for the whole chunk apply: `apply_reason`, `block_height`, `prev_block_hash`, `shard_id`, `epoch_id`, `epoch_height`, `gas_price`, `block_timestamp`, `gas_limit`, `random_seed`, `current_protocol_version`, `config: Arc<RuntimeConfig>`, `next_wasm_config`, `cache`, `trie_access_tracker_state`, `is_new_chunk`, `save_receipt_to_tx`, `congestion_info`, `bandwidth_requests`, `on_post_state_ready`. `create_receipt_id` derives child receipt IDs from a parent ID + block height + index (`runtime/runtime/src/lib.rs:223`).
- **`ApplyResult`** — `runtime/runtime/src/lib.rs:388` — outputs: `state_root`, `trie_changes`, `validator_proposals`, `outgoing_receipts`, `outcomes`, `state_changes`, `stats`, `processed_receipts`, `processed_yield_timeouts`, `proof`, `delayed_receipts_count`, `congestion_info`, `bandwidth_requests`, `bandwidth_scheduler_state_hash`, `contract_updates`, `receipt_to_tx`.
- **`ActionResult`** — `runtime/runtime/src/lib.rs:412` — per-action outcome: `gas_burnt`, `gas_burnt_for_function_call`, `gas_used`, `compute_usage`, `result: Result<ReturnData, ActionError>`, `logs`, `new_receipts`, `validator_proposals`, `profile`, `current_contract` (contract on the receiver observed *before* the action; feeds `ExecutionMetadata::V4`), `tokens_burnt`, `subsidized_amount`.
- **`ActionReceiptResult`** — `runtime/runtime/src/lib.rs:454` — receipt-level fold of per-action `ActionResult`s via `merge` (`runtime/runtime/src/lib.rs:487`). `merge` asserts `gas_burnt_for_function_call <= gas_burnt <= gas_used`, sums gas/compute/profile, pushes `current_contract` onto `current_contracts`, and on error calls `set_error` (`runtime/runtime/src/lib.rs:535`) which clears queued `new_receipts`, `validator_proposals`, `tokens_burnt`, `subsidized_amount` — i.e. a failing receipt discards receipt-scoped side effects but keeps gas counters, profile, logs and `current_contracts`.
- **`TxVerdict`** — `runtime/runtime/src/lib.rs:287` — result of tx verification: `Success(VerificationResult)`, `DepositFailed { result, error }` (gas-key path: gas charged, deposit not covered), `Failed(InvalidTxError)` (no state change).
- **`VerificationResult`** / **`AccessKeyUpdate`** — `runtime/runtime/src/lib.rs:298` / `:318` — the computed charge (`gas_burnt`, `compute_burnt`, `gas_remaining`, `receipt_gas_price`, `burnt_amount`, `new_account_amount`) plus how to mutate the key: `Regular { nonce, new_allowance }`, `GasKey { new_balance, nonce_index, nonce }`, or `Bootstrap { nonce }` (universal-account self-signed state init; the nonce lives on the *account*). Verification functions do not mutate state; callers apply via `VerificationResult::apply` (`runtime/runtime/src/lib.rs:337`).
- **`TxAuthorization`** — `runtime/runtime/src/verifier.rs:140` — what authorizes a transaction: `AccessKey`, `GasKey { access_key, nonce_index }`, or `SelfSignedStateInit`. Resolved by `get_signer_and_authorization` (`runtime/runtime/src/verifier.rs:164`).
- **`PendingConstraints`** — `runtime/runtime/src/lib.rs:248` — balance/nonce commitments from included-but-uncertified transactions; **always default (zero) on the consensus path**, best-effort on the RPC / chunk-production path (`runtime/runtime/src/verifier.rs:353`).
- **`TransactionCost`** — `runtime/runtime/src/config.rs:22` — output of `tx_cost` / `calculate_tx_cost`: `gas_burnt`, `compute_burnt`, `gas_remaining`, `receipt_gas_price`, `burnt_amount`, `gas_cost`, `deposit_cost`, `total_cost`.
- **`GasRefundResult`** — `runtime/runtime/src/lib.rs:546` — `price_deficit`, `price_surplus`, `refund_penalty`, `create_account_charge`.
- **`ReceiptShape`** — `runtime/runtime/src/actions.rs:807` — the two bits of the enclosing receipt that gate implicit account creation: `is_refund`, `is_the_only_action`.
- **`ValidatorAccountsUpdate`** — `runtime/runtime/src/lib.rs:234` — per-epoch-boundary stake/reward info consumed by `update_validator_accounts`.
- **`UniversalStateInit`** — `core/primitives/src/universal_state_init.rs:68` (`V1`) — `code: Option<GlobalContractIdentifier>`, `data: BTreeMap<Vec<u8>, Vec<u8>>`, `access_keys: BTreeSet<PublicKeyHandle>`. Carried on the wire as `RawStateInit` (opaque bytes); `from_raw` (`:116`) accepts any well-formed borsh, canonicity is **not** checked — a non-canonical encoding simply hashes to a different account id.

## Behavior

### 1. Ordered phases of `apply`

`Runtime::apply` (`runtime/runtime/src/lib.rs:1860`) documents four conceptual steps (validator accounts → transactions → receipts → validate/apply state). Concretely, in order:

1. **Build `ApplyProcessingState`** and **prefetch** transaction data (best-effort; failure ignored) (`runtime/runtime/src/lib.rs:1884`, `:1889`).
2. **Update validator accounts** if a `ValidatorAccountsUpdate` is supplied (`runtime/runtime/src/lib.rs:1894` → `update_validator_accounts` `:1717`). This runs first so reward/stake changes land before any tx/receipt reads balances; it commits with `StateChangeCause::ValidatorAccountsUpdate` (`runtime/runtime/src/lib.rs:1835`).
3. **Load the delayed receipt queue** (`runtime/runtime/src/lib.rs:1902`).
4. **Run the bandwidth scheduler** — for every chunk, including missing ones (`runtime/runtime/src/lib.rs:1909` — `run_bandwidth_scheduler`).
5. **Missing-chunk short-circuit**: if `!apply_state.is_new_chunk`, finalize immediately and return without processing any receipts (`runtime/runtime/src/lib.rs:1917` → `missing_chunk_apply_result` `:3064`), carrying congestion info and bandwidth requests forward unchanged and emitting no outcomes/outgoing receipts.
6. **Build the `ReceiptSink`** from own congestion info + scheduler output, then **forward buffered receipts** from prior chunks first (`runtime/runtime/src/lib.rs:1930`, `:1938`). See [cross-shard congestion](cross-shard-congestion.md).
7. **Process transactions** → local receipts / forwarded receipts (`runtime/runtime/src/lib.rs:1941` — `process_transactions` `:2011`).
8. **Process receipts** in the order local → delayed → incoming, then resolve promise-yield timeouts (`runtime/runtime/src/lib.rs:1945` — `process_receipts` `:2787`).
9. **Finalize**: `validate_apply_state_update` (`runtime/runtime/src/lib.rs:1955` / `:2852`) — persist promise-yield indices, apply delayed-receipt congestion changes and choose the allowed shard, generate bandwidth requests, commit with `UpdatedDelayedReceipts`, apply the sandbox state patch, invoke `on_post_state_ready` if set, run `state_update.finalize()`, dedup validator proposals (last proposal per account wins; iteration is reversed so the order is deterministically changed) (`runtime/runtime/src/lib.rs:2944`), and assemble `ApplyResult`.

The local → delayed → incoming order is load-bearing: local receipts (from this chunk's transactions) and the delayed backlog are drained before new incoming cross-shard receipts, so backlog is preferred over new work, and gas-limit exhaustion pushes the *newest* work onto the delayed queue rather than starving the backlog (`runtime/runtime/src/lib.rs:2794`-`2820`).

### 2. Transaction validation and conversion (`process_transactions`, `runtime/runtime/src/lib.rs:2011`)

1. **Parallel phase** (`rayon::join`, `runtime/runtime/src/lib.rs:2022`): one arm stateless-validates every tx via `validate_transaction` (`runtime/runtime/src/verifier.rs:109`) — action validation (`validate_actions`, see §3) then `ValidatedTransaction::new` (signature, version, key-scheme admission); expired txs short-circuit to `InvalidTxError::Expired`. The other arm prefetches signer accounts, access keys and gas-key nonces into `DashMap`s (`runtime/runtime/src/lib.rs:2052`-`2104`).
2. **Sequential phase**, per tx:
   - `UniqueChunkTransactions` (v85): skip any repeat of a tx hash already seen in this chunk — a tx hash is also its outcome id, so a duplicate would commit two conflicting outcomes under one id (`runtime/runtime/src/lib.rs:2110`).
   - Stateless-validation failure → failed outcome pushed, tx skipped (`runtime/runtime/src/lib.rs:2124`).
   - `tx_cost` (`runtime/runtime/src/config.rs:464`); overflow → `CostOverflow` failed outcome (`runtime/runtime/src/lib.rs:2135`).
   - Missing signer account → `InvalidSignerId` (`runtime/runtime/src/lib.rs:2159`). Missing access key → `AccessKeyNotFound`, **unless** `is_bootstrap(account, tx)` holds (`runtime/runtime/src/lib.rs:2174`, `runtime/runtime/src/verifier.rs:200`).
   - **Charge**, by authorization kind (`runtime/runtime/src/lib.rs:2196`): no access key → `verify_and_charge_bootstrap_tx_ephemeral` (`runtime/runtime/src/verifier.rs:424`); nonce carries a `nonce_index` → `verify_and_charge_gas_key_tx_ephemeral` (`runtime/runtime/src/verifier.rs:524`); otherwise `verify_and_charge_tx_ephemeral` (`runtime/runtime/src/verifier.rs:312`). All three are pure and return a `TxVerdict`.
3. **`TxVerdict::Success`** → build the receipt with `Receipt::from_tx` at `result.receipt_gas_price` (`runtime/runtime/src/lib.rs:2281`); if `receiver_id == signer_id` it is a **local receipt**, otherwise it is handed to `ReceiptSink::forward_or_buffer_receipt` (`runtime/runtime/src/lib.rs:2317`-`2325`). Outcome status is `SuccessReceiptId`, metadata `ExecutionMetadata::V1` (transactions carry no profile).
4. **`TxVerdict::DepositFailed`** (gas-key only): gas is still burnt against the gas key, a `failed_with_gas_burnt` outcome is emitted, no receipt is created (`runtime/runtime/src/lib.rs:2258`). **`TxVerdict::Failed`**: failed outcome, tx skipped, no state change (`runtime/runtime/src/lib.rs:2329`).
5. Chunk-level `tx_burnt_amount` is accumulated; an overflow drops the tx *without* an outcome (`runtime/runtime/src/lib.rs:2354`). Gas + compute are added to the chunk totals (`runtime/runtime/src/lib.rs:2364`).
6. **State write-back**: `result.apply` mutates the in-memory signer account + access key (`runtime/runtime/src/lib.rs:2367`), then `set_account`, the gas-key nonce (`set_gas_key_nonce`, also refreshing the prefetch cache so later txs in the chunk see it) and `set_access_key` are written, and the update commits with `StateChangeCause::TransactionProcessing` (`runtime/runtime/src/lib.rs:2368`-`2397`). On the bootstrap path nothing is written to the key store — the key does not exist yet and the state init is what creates it.

**Failed-transaction outcomes are recorded unconditionally.** The old `register_outcome` helper and its `InvalidTxGenerateOutcomes` gate are gone; every branch pushes its outcome directly (`runtime/runtime/src/lib.rs:2125`, `:2143`, `:2332`), and a `debug_assert` requires `outcomes.len() == num_transactions - num_skipped_duplicate_txs` (`runtime/runtime/src/lib.rs:2399`).

**Send fees are burnt here, on the signer's shard, at conversion time.** `calculate_tx_cost` (`runtime/runtime/src/config.rs:481`) sets `burnt = new_action_receipt send_fee + total_send_fees(actions) + signature_verification_cost`, burnt at `current_gas_price`; `gas_remaining` (prepaid attached gas + prepaid send fees + receipt exec fee + prepaid exec fees) is attached to the receipt and purchased at `receipt_gas_price = max(current_gas_price, min_gas_purchase_price)` (`runtime/runtime/src/config.rs:532`). `sender_is_receiver` ("sir") selects the cheaper send-fee column (`runtime/runtime/src/config.rs:489`).

### 3. Action validation (`action_validation.rs`)

`validate_actions_with_mode` (`runtime/runtime/src/action_validation.rs:86`) runs on transactions (via `validate_transaction`) and on receipts (via `validate_receipt`). `ValidateReceiptMode` (`runtime/runtime/src/verifier.rs:728`) distinguishes `NewReceipt` (strict; transactions and freshly created receipts) from `ExistingReceipt` (receipts already in flight or stored as delayed). Checks, in order:

1. `max_actions_per_receipt` (`:93`).
2. Post-quantum gate: if `PostQuantumSignatures` (v85) is off, any action requiring it is rejected with `UnsupportedProtocolFeature` (`:108`). This is the **centralized** gate — it also covers actions emitted by contracts, which never pass tx admission.
3. `NewReceipt` only: `max_deploy_actions_per_receipt` and `max_state_init_entries` over *all* state-init actions in the receipt combined (`validate_number_of_state_init_entries`, `:45`).
4. `DeleteAccount` must be the final action; at most one delegate action per receipt (`:128`-`:137`).
5. Per-action validation (`validate_action_with_mode`, `:157`), then `max_total_prepaid_gas` (`:145`).

Per-action rules that matter at v87: `FunctionCall` must have non-zero gas and (with `RejectEmptyMethodName`, `NewReceipt` only) a non-empty `method_name` (`:311`-`:326`); `DelegateV2` requires `DelegateV2` (v85) and, for `NewReceipt`, is refused by `reject_removed_protocol_feature` once `RejectDelegateV2` is on (`:186`-`:199`); `validate_delegate_action` bounds the nested action count *before* cloning the list, refuses a nested `WithdrawFromGasKey` under `RejectWithdrawFromGasKeyInDelegate` (`NewReceipt` only), and validates the inner actions against the delegate's own `receiver_id` (`:222`-`:268`); `UniversalStateInit` requires `UniversalAccounts`, pins the receiver to `derive_universal_account_id(raw bytes)`, requires the payload to decode, and bounds `access_keys` by `max_universal_state_init_keys` (1 024) and each storage key/value by the trie limits (`:535`-`:588`).

### 4. Nonce, balance and storage checks (`verifier.rs`)

- `verify_nonce` (`runtime/runtime/src/verifier.rs:254`): `Monotonic` requires `tx_nonce > current_nonce`; `Strict` (opt-in via `NonceMode`, gated by `StrictNonce`, v85) requires `tx_nonce == current_nonce + 1`. Both enforce `tx_nonce < block_height * ACCESS_KEY_NONCE_RANGE_MULTIPLIER`.
- `verify_and_charge_tx_ephemeral` (`runtime/runtime/src/verifier.rs:312`): asserts the caller routed gas-key txs elsewhere; rejects a gas-key access key used without a nonce index (`InvalidNonceIndex`); checks nonce against `max(access_key.nonce, pending.max_nonce)`; checks balance (`NotEnoughBalance`); computes the new allowance (`check_and_compute_new_allowance` `:282`) — **unconditionally non-mutating**, the returned `AccessKeyUpdate` is the only carrier; checks storage staking on the post-charge amount (`check_storage_stake` `:48`); finally verifies function-call-permission constraints (`verify_function_call_permission` `:209`).
- `verify_and_charge_gas_key_tx_ephemeral` (`runtime/runtime/src/verifier.rs:524`): requires the key to be a gas key, `nonce_index < num_nonces`, a valid nonce, and gas-key balance ≥ `gas_cost`; it also pre-computes the balance left if only `burnt_amount` is charged. Then it checks the *account* balance against `deposit_cost` and storage staking — a shortfall yields `DepositFailed` (gas key charged only the conversion burn), not a hard failure.
- `verify_and_charge_bootstrap_tx_ephemeral` (`runtime/runtime/src/verifier.rs:424`): the one transaction an account can send with no access key. It re-derives authorization rather than trusting the caller: the account must carry a `bootstrap_nonce` (only uninitialized accounts do), `universal_accounts` must be on, and `tx.is_state_init_bootstrap()` must hold — signer == receiver, no `nonce_index`, and a `UniversalStateInit` action whose derived id equals the account and whose `access_keys` contain the signing key's handle (`core/primitives/src/transaction.rs:236` — `Transaction::state_init_bootstrap`). The nonce is checked in **`Strict` mode regardless of what the transaction asked for**, so exactly one nonce is admissible and the same signed bytes cannot replay through the ordinary key path once the state init installs the keys.
- `check_storage_stake` (`runtime/runtime/src/verifier.rs:48`): zero-balance accounts (≤ `ZERO_BALANCE_ACCOUNT_STORAGE_LIMIT = 770` bytes, `:25`) are exempt (`is_zero_balance_account` `:88`).

### 5. Action receipt execution (`apply_action_receipt`, `runtime/runtime/src/lib.rs:827`)

1. **Promise-input size check** (new at v87). When `ReceiptPromiseInputSizeLimit` is on, the sizes of all `input_data_ids` are summed from state and compared with `max_receipt_total_input_size` (`runtime/runtime/src/lib.rs:847`-`858`). If exceeded, the inputs are removed from state, `promise_results` is empty, **no action runs at all**, and the receipt fails with `ActionErrorKind::TotalPromiseInputSizeExceeded { size, limit }` (`runtime/runtime/src/lib.rs:918`).
2. Otherwise gather each `input_data_id` as a `PromiseResult` and remove it from state; a missing entry is `StorageInconsistentState` (`runtime/runtime/src/lib.rs:862`-`897`).
3. Commit prior updates with `ActionReceiptProcessingStarted` (`runtime/runtime/src/lib.rs:903`).
4. Load the receiver account (remembering whether it existed) and seed the receipt-level result with the `new_action_receipt` exec fee (`runtime/runtime/src/lib.rs:907`-`913`).
5. Snapshot `recorded_storage_size_upper_bound` before the receipt when `EnforcePerReceiptStorageProofLimit` (v86) is on, and compute the per-action storage-proof limit when `EnforceStorageProofLimitForAllActions` (v87) is on (`runtime/runtime/src/lib.rs:925`-`941`).
6. **Execute actions in order** (`runtime/runtime/src/lib.rs:946`): derive `action_hash` from receipt id + block height + index, call `apply_action`, and on success validate every newly created receipt with `validate_receipt(..., NewReceipt)` — a failure becomes `ActionErrorKind::NewReceiptValidationError` (`:961`-`:975`). `merge` folds the result. Then, if the receipt is still `Ok` and both storage-proof values are present, the growth attributable to this receipt is compared against `per_receipt_storage_proof_size_limit` (4 MB) and, if over, the receipt fails with `ActionErrorKind::ReceiptStorageProofSizeExceeded { limit }` (`:982`-`998`). On the first `Err` the action index is recorded and the loop breaks — **later actions do not run** (`:1000`).
7. If the receipt still succeeded, re-check receiver storage staking; a shortfall sets `LackBalanceForState`, a storage error is fatal (`runtime/runtime/src/lib.rs:1008`-`1032`).
8. **Refunds**: system-predecessor receipts (refunds) are free — no refund is generated, and a failed refund burns its deposit into `other_burnt_amount` (`runtime/runtime/src/lib.rs:1047`-`1050`). Otherwise `refund_unspent_gas_and_deposits` runs, told whether this receipt created a new account (`:1061`).
9. **Commit or rollback**: `Ok` commits with `ReceiptProcessing`; `Err` calls `state_update.rollback()`, discarding every state change made by the receipt (`runtime/runtime/src/lib.rs:1079`-`1088`).
10. **`tx_burnt_amount`** = `gas_burn_price * gas_burnt − price_deficit (+ price_surplus pre-`AccountCostIncrease`) + refund_penalty + create_account_charge + tokens_burnt` (`runtime/runtime/src/lib.rs:1093`-`1103`); refund receipts contribute zero burnt gas. The outcome's `tokens_burnt` is this pre-reward value.
11. **Function-call gas reward**: `receiver_gas_reward = gas_burnt_for_function_call * burnt_gas_reward`; if non-zero and the receiver account still exists, it is credited to the account and subtracted from `tx_burnt_amount`, committed with `ActionReceiptGasReward` (`runtime/runtime/src/lib.rs:1108`-`1139`). **At v87 `burnt_gas_reward` is 0** (`RemoveGasRewards`, `core/parameters/res/runtime_configs/87.yaml:3`), so this branch never fires and validators keep the whole burn.
12. **Output data**: if the receipt has `output_data_receivers`, either the returned `ReceiptIndex` receipt absorbs them or `Data` receipts carrying the return value (`None` on failure) are appended (`runtime/runtime/src/lib.rs:1152`-`1190`).
13. **Emit new receipts**: assign receipt ids; record `receipt_to_tx` origins when `save_receipt_to_tx` is set; instant receipts go to the `instant_receipts` queue (drained immediately after the current receipt by `process_receipt_and_instant_receipts` `:2748`), others are forwarded/buffered via the `ReceiptSink` (`runtime/runtime/src/lib.rs:1193`-`1237`). Only action-kind children appear in the outcome's `receipt_ids`.
14. **Build the outcome** (`runtime/runtime/src/lib.rs:1240`): status is `SuccessReceiptId` / `SuccessValue` / `Failure(ActionError)`. Metadata is `ExecutionMetadata::V4` (per-action `Vec<AccountContract>`, padded with `AccountContract::None` to the action count) when `ExecutionMetadataV4` (v85) is on, else `V3` (profile only) (`runtime/runtime/src/lib.rs:1252`-`1259`).

### 6. Per-action execution (`apply_action`, `runtime/runtime/src/lib.rs:571`)

`apply_action` seeds `ActionResult` with the action's `exec_fee` (gas + compute, `runtime/runtime/src/lib.rs:588`) and captures `current_contract` before running anything. It builds `ReceiptShape { is_refund: predecessor.is_system(), is_the_only_action: actions.len() == 1 }` (`:597`), then runs `check_account_existence` (`runtime/runtime/src/actions.rs:814`) and `check_actor_permissions` (`runtime/runtime/src/actions.rs:755`); either failure returns early with the error set and the exec fee already burnt. Dispatch:

| Action | Handler | Effect |
| --- | --- | --- |
| `CreateAccount` | `action_create_account` `runtime/runtime/src/actions.rs:166` | Creates an empty account; top-level length / sub-account naming rules. Refused outright for any *implicit* id, with `ActionErrorKind::OnlyImplicitAccountCreationAllowed` (`actions.rs:836`). |
| `DeployContract` | `action_deploy_contract` `runtime/runtime/src/actions.rs:290` | Clears old contract storage usage, sets the local code hash, updates storage usage, records the deploy (`state_update.record_contract_deploy`, `actions.rs:326`) and precompiles. |
| `DeployGlobalContract` | `action_deploy_global_contract` `runtime/runtime/src/global_contracts.rs:25` | Publishes a global contract and starts its cross-shard distribution; burnt storage is counted into the outcome unconditionally. |
| `UseGlobalContract` | `action_use_global_contract` `runtime/runtime/src/global_contracts.rs:65` | Points the account at a global contract (`use_global_contract` `:76`). |
| `DeterministicStateInit` | `action_deterministic_state_init` `runtime/runtime/src/deterministic_account_id.rs:15` | NEP-616 `0s` accounts: create-if-absent, deploy on the `uninit → active` transition, then `settle_state_init_deposit`. |
| `UniversalStateInit` | `action_universal_state_init` `runtime/runtime/src/universal_account_id.rs:21` | `0u` accounts — see §8. |
| `FunctionCall` | `action_function_call` `runtime/runtime/src/function_call.rs:31` | Resolves the contract (`RuntimeContractIdentifier::resolve`), takes the prepared contract from the pipeline, invokes the [contract VM](contract-vm.md); records burnt/used gas, compute, logs, subsidized amount, new receipts and yield-timeout queue entries. |
| `Transfer` | `action_transfer_or_implicit_account_creation` `runtime/runtime/src/lib.rs:3016` | Credits balance; for gas refunds routes to the gas-key balance first (`try_refund_gas_key_balance`) and otherwise tops up the key allowance (`try_refund_allowance`); if the account is absent, creates the implicit account (`action_implicit_account_creation_transfer` `runtime/runtime/src/actions.rs:221`). |
| `Stake` | `action_stake` `runtime/runtime/src/actions.rs:58` | Emits a `ValidatorStake` proposal after the minimum-stake check and moving amount↔locked. |
| `AddKey` / `DeleteKey` | `action_add_key` `runtime/runtime/src/access_keys.rs:149` / `action_delete_key` `:52` | Adds/removes an access key or gas key (with its per-nonce sub-entries). |
| `DeleteAccount` | `action_delete_account` `runtime/runtime/src/actions.rs:330` | Refunds remaining balance to the beneficiary, burns gas-key balances into `tokens_burnt`, charges compute for nonce removals, removes the account; requires zero locked stake (`check_actor_permissions`) and bounded state. |
| `Delegate` / `DelegateV2` | `apply_delegate_action` `runtime/runtime/src/actions.rs:453` | Meta-transactions (NEP-366): verify the inner signature, check expiry and sender, advance the inner key's nonce, and spawn a receipt carrying the inner actions. |
| `TransferToGasKey` / `WithdrawFromGasKey` | `action_transfer_to_gas_key` `runtime/runtime/src/access_keys.rs:257` / `action_withdraw_from_gas_key` `:290` | Move balance to/from a gas key. |

Implicit account creation on transfer is decided by `implicit_creation_allowed` (`runtime/runtime/src/actions.rs:929`): never for a refund, never for a named id; NEAR-implicit / ETH-implicit / `0s` deterministic ids require the transfer to be the receipt's **only** action (anti-hijacking); **`0u` universal ids are the exception and may be created by a batched transfer** because a universal id commits to its own key set and cannot be hijacked by a co-batched action. `get_account_type` (`runtime/runtime/src/actions.rs:899`) resolves the account type *through the runtime config flags* (`eth_implicit_accounts`, `universal_accounts`) so a disabled type reads as `NamedAccount` rather than silently upgrading the protocol.

### 7. Gas accounting and refunds (`refund_unspent_gas_and_deposits`, `runtime/runtime/src/lib.rs:1284`)

Under NEP-536 (always on — the flag is `_DeprecatedReducedGasRefunds`, v78) gas is purchased once for the whole execution and price changes do not by themselves create refunds. The function computes `prepaid_gas` (attached gas + prepaid send fees) and `prepaid_exec_gas` (prepaid exec fees + the receipt exec fee). On failure the full deposit is refunded and `gross_gas_refund = prepaid − gas_burnt`; on success `gross_gas_refund = prepaid − gas_used` (`:1304`-`:1316`). The NEP-536 penalty `gas_penalty_for_gas_refund` is deducted (`:1319`). A deposit-refund receipt (`Receipt::new_balance_refund`, to `receipt.balance_refund_receiver()`) and/or a gas-refund receipt (`Receipt::new_gas_refund`, which also credits the signing key's allowance) are pushed to `new_receipts` (`:1400`-`:1414`).

**`AccountCostIncrease` (v85)** changes the price model: `gas_burn_price = min(gas_purchase_price, apply_state.gas_price)` instead of the current price (`runtime/runtime/src/lib.rs:1038`-`1046`); the penalty is priced at the burn price rather than the purchase price (`:1318`); the price *surplus* (purchase > burn) becomes a `burned_gas_refund` to the signer instead of being folded into `tx_burnt_amount` (`:1352`, `:1096`); and creating an account triggers `create_account_charge` — top-up to `account_creation_charge`, capped by the available burned-gas refund (`:1360`-`1393`).

### 8. Universal accounts (`0u`, `UniversalAccounts` at v87)

A `0u` account id is `SHA3-256` over the **exact wire bytes** of the state init, encoded with the UAID codec (`core/primitives/src/utils.rs:502` — `derive_universal_account_id`). The derivation deliberately takes `RawStateInit` rather than a decoded value: canonicity is not checked anywhere, a non-canonical borsh encoding simply derives a different id (`core/primitives/src/universal_state_init.rs:116`).

Three creation paths converge on the same account:

1. **Transfer.** A transfer to an absent `0u` id creates an **uninitialized** account — `Account::new_uninitialized` with a bootstrap nonce seeded from the block height (`runtime/runtime/src/actions.rs:276`, `core/primitives-core/src/account.rs:283`). This may be batched with other actions (§6). An uninitialized account has no keys, code or data, and `check_account_existence` rejects every action against it except its own state init and a further transfer, with `ActionErrorKind::AccountNotInitialized` (`runtime/runtime/src/actions.rs:884`).
2. **`UniversalStateInit` action** (`runtime/runtime/src/universal_account_id.rs:21`). Creates the account if absent (without changing `actor_id`, so a co-batched follow-up cannot hijack it), and on an uninitialized account decodes the payload, marks it initialized, and installs state via `install_universal_account` (`:102`): optional global contract code, storage entries, then full-access keys stored by on-trie handle. Every installed key gets nonce `max(initial_nonce_value(block_height), consumed bootstrap nonce)` so the bootstrap transaction's bytes cannot replay through the key path. Storage usage is accumulated per record. Finally `settle_state_init_deposit` (`runtime/runtime/src/deterministic_account_id.rs:73`, shared with the `0s` path) tops up exactly the missing storage stake and refunds the rest, or fails with `LackBalanceForState`. On an already-initialized account only the deposit is settled — the existing state is untouched.
3. **Self-signed bootstrap transaction.** See §4: the account id itself is the authorization, verified by `verify_and_charge_bootstrap_tx_ephemeral`.

Caps: `max_universal_state_init_keys = 1_024` on committed access keys (`runtime/runtime/src/action_validation.rs:566`), and `max_state_init_entries = 1_500` on the combined storage entries of all state-init actions in a *new* receipt (`runtime/runtime/src/action_validation.rs:45`, value in `core/parameters/res/runtime_configs/87.yaml`). The entry cap applies to `DeterministicStateInit` too, and only in `NewReceipt` mode so receipts already in flight at the upgrade keep executing. Fees are computed by `universal_state_init_fee` (`runtime/runtime/src/config.rs:242`) on the payload's own byte length plus decoded entry/key counts, shared with the VM host function that lets a contract build the action.

### 9. Execution outcome / status

Every processed transaction and receipt yields an `ExecutionOutcomeWithId` (`runtime/runtime/src/lib.rs:1261` for receipts, `:2289` for transactions) recording `status`, `logs`, `receipt_ids`, `gas_burnt`, `compute_usage`, `tokens_burnt`, `executor_id`, `metadata`. Status is one of `SuccessValue`, `SuccessReceiptId`, or `Failure(TxExecutionError)`.

### 10. Gas/compute limit and delayed receipts

`process_receipts` sets `compute_limit = gas_limit` (`runtime/runtime/src/lib.rs:2797`; there is still no separate compute limit, TODO #8859). In each of `process_local_receipts` / `process_delayed_receipts` / `process_incoming_receipts`, before processing a receipt the loop checks `total.compute >= compute_limit || trie.check_proof_size_limit_exceed()`; if exceeded, the receipt is pushed onto the delayed queue instead of executing (local `runtime/runtime/src/lib.rs:2514`, incoming `:2704`) or the delayed loop breaks (`:2595`). Incoming receipts are `validate_receipt(..., ExistingReceipt)`-checked **before** either branch, so an invalid receipt is never stored as delayed (`runtime/runtime/src/lib.rs:2696`). Contract preparation is scheduled one receipt ahead in every loop (`schedule_contract_preparation`, `runtime/runtime/src/lib.rs:3383`). Whichever limit stopped the chunk is recorded in `CHUNK_RECEIPTS_LIMITED_BY` (`runtime/runtime/src/lib.rs:2829`).

Promise-yield timeouts are resolved last and obey the same limits (`resolve_promise_yield_timeouts`, `runtime/runtime/src/lib.rs:3113`): expired queue entries whose yield receipt is still present emit a local `PromiseResume` receipt with `data: None`, and the updated indices are persisted in `validate_apply_state_update`.

### 11. Validator accounts update (`update_validator_accounts`, `runtime/runtime/src/lib.rs:1717`)

For each validator with stake info: an **uninitialized** account is skipped, but with a defence-in-depth check that its stake/reward/proposal are all zero, else `StorageInconsistentState` (`:1730`-`:1747`). Otherwise add the reward to `locked`, assert `locked >= max_of_stakes`, compute `return_stake = locked − max(max_of_stakes, last_proposal)`, and move it from `locked` back to `amount` (`:1749`-`:1795`). A missing account with positive `max_of_stakes` is fatal (`:1796`). The protocol treasury account gets its reward if it did not already receive one as a validator (`:1807`). Commits with `ValidatorAccountsUpdate`.

## Interactions

- **Consumes**: pre-state `Trie`, `ApplyState`, incoming receipts, `SignedValidPeriodTransactions`, optional `ValidatorAccountsUpdate`, `EpochInfoProvider` (min stake, shard layout, chain id), `RuntimeConfig`. Config/fees come from [economics](economics.md); the validator update comes from [epoch, validators & staking](epoch-validators-staking.md).
- **Produces**: new state root + `TrieChanges` → [state storage](state-storage.md); outgoing receipts (via `ReceiptSink`) and bandwidth requests → [cross-shard congestion control](cross-shard-congestion.md); `ValidatorStake` proposals → [epoch, validators & staking](epoch-validators-staking.md); execution outcomes → chunk outcome root / RPC; recorded storage → [stateless validation](stateless-validation.md).
- **Invokes**: the [contract VM](contract-vm.md) for `FunctionCall`; contract preparation is pipelined (`runtime/runtime/src/pipelining.rs`) and `receipt_manager.rs` collects VM-created receipts. Wire types for receipts/actions live in [data structures & serialization](data-structures-serialization.md); account and key semantics in [accounts & keys](accounts-keys.md).

## Protocol-version-gated behavior

Verified against `core/primitives-core/src/version.rs` in this tree: `STABLE_PROTOCOL_VERSION = 87` (`version.rs:680`), `MIN_SUPPORTED_PROTOCOL_VERSION = 84` (`version.rs:652`), feature→version map at `version.rs:500` — `ProtocolFeature::protocol_version`.

### New at 87

| Feature | `version.rs` | Before → after | Enforcement site |
| --- | --- | --- | --- |
| `RejectEmptyMethodName` | decl `:452`, map `:620` | A `FunctionCall` with `method_name == ""` was accepted (and resolved to a method-not-found abort at execution) → rejected during action validation with `ActionsValidationError::FunctionCallEmptyMethodName`, `NewReceipt` mode only. | `runtime/runtime/src/action_validation.rs:322` — `validate_function_call_action` |
| `RejectDelegateV2` | decl `:460`, map `:621` | `Action::DelegateV2` (v85) was a valid meta-transaction carrier including from gas keys → new transactions/receipts carrying it are rejected with `ActionsValidationError::RemovedProtocolFeature`. Receipts created before the upgrade keep executing, so in-flight meta-transactions complete. The variant and `VersionedDelegateActionPayload` are retained for a future delegate version. Combined with `Delegate`'s existing `DelegateActionRequiresNonGasKey` rejection (`runtime/runtime/src/actions.rs:610`), **gas keys can no longer sign meta-transactions at all**. | `runtime/runtime/src/action_validation.rs:193` — `validate_action_with_mode` / `reject_removed_protocol_feature` `:483` |
| `RejectWithdrawFromGasKeyInDelegate` | decl `:465`, map `:622` | A `WithdrawFromGasKey` nested inside a delegate action executed normally → rejected with `ActionsValidationError::WithdrawFromGasKeyNotAllowedInDelegate` (`NewReceipt` only; in-flight receipts still complete). `WithdrawFromGasKey` remains valid as a **top-level** action; only the nesting is refused. | `runtime/runtime/src/action_validation.rs:244` — `validate_delegate_action` |
| `EnforceStorageProofLimitForAllActions` | decl `:473`, map `:624` | The in-VM `RecordedStorageCounter` bounded `FunctionCall` only, so other actions in the same receipt could record proof past the limit → the receipt's recorded growth is re-checked **after every action** against `per_receipt_storage_proof_size_limit` (4 MB, set at v69); the first action to go over fails the receipt with `ActionErrorKind::ReceiptStorageProofSizeExceeded` and the actions after it do not run. | `runtime/runtime/src/lib.rs:982` — `apply_action_receipt` |
| `ReceiptPromiseInputSizeLimit` | decl `:450`, map `:625` | The combined size of a receipt's resolved promise inputs was effectively unbounded (it is read into the state witness before the per-receipt storage-proof limit starts counting) → bounded by the new `max_receipt_total_input_size = 4_194_944` bytes; over it the inputs are dropped, **no action executes**, and the receipt fails with `ActionErrorKind::TotalPromiseInputSizeExceeded`. | `runtime/runtime/src/lib.rs:848` — `apply_action_receipt`; limit in `core/parameters/res/runtime_configs/87.yaml` |
| `RemoveGasRewards` | decl `:477`, map `:623` | 30 % of the gas burnt by a `FunctionCall` was credited back to the contract account → `burnt_gas_reward` parameter goes 3/10 → 0/1, so `receiver_gas_reward` is always zero and validators keep the full burn. | Parameter diff `core/parameters/res/runtime_configs/87.yaml:3`; consumption at `runtime/runtime/src/lib.rs:1108` |
| `FixMlDsaCostCharging` | decl `:487`, map `:627` | (a) Gas-key fees are made to agree on both creation paths: send (transmission) on the wire length `len()`, exec (storage) on the on-trie identifier length `trie_id_len()`. On the **transaction** path the send length moves `trie_id_len()` → `len()` (exec was already `trie_id_len()`: `runtime/runtime/src/config.rs:411`, `:456`); on the **host-function** path the exec length moves `len()` → `trie_id_len()` (the send fee is `len()` either way). (b) The inner `DelegateAction` signature-verification **compute** was metered on the signer shard, which never runs the verify → metered on the receiver shard against its `compute_limit`. Both are no-ops for ed25519/secp256k1, where the two lengths coincide; they differ only for ML-DSA-65 (1953 wire vs 33 on-trie). Surfaced as the `fix_ml_dsa_cost_charging` wasm-config flag (`87.yaml:35`). | `runtime/runtime/src/config.rs:73` — `gas_key_send_pk_len`; `runtime/near-vm-runner/src/logic/logic.rs:194` — `gas_key_exec_pk_len`; `runtime/runtime/src/config.rs:589` — `signature_verification_cost`; `runtime/runtime/src/actions.rs:467` — `apply_delegate_action` |
| `FixContractLoadingError` | decl `:448`, map `:619` | A failure in `Module::deserialize` surfaced as `VMRunnerError::LoadingError` and finalized as a zero-gas nop outcome → it is turned into a cached `FunctionCallError::LoadingError` that flows through the fee-charge points, so the caller pays `contract_loading_base` + `contract_loading_bytes` × code length and the call finalizes as a gas-bearing abort. The call failed either way; only who pays for the loading work changes. Surfaced as the `fix_contract_loading_error` wasm-config flag. | `runtime/near-vm-runner/src/wasmtime_runner/mod.rs:753` — module deserialization |
| `GlobalContractSameChunkCallFix` | decl `:413`, map `:628` | A global contract distributed in the current chunk was not visible to a call in the same chunk without a warm compilation cache → the distribution records the deploy in the trie update, so the same-chunk call resolves. | `runtime/runtime/src/global_contracts.rs:219` — `apply_distribution_current_shard` |
| `UniversalAccounts` | decl `:491`, map `:629` | The `0u` account scheme did not exist → `UniversalStateInit` becomes a valid action, `0u` ids are implicit (creatable by transfer, including a batched one), and uninitialized accounts plus the self-signed bootstrap transaction path become reachable. See §8. Surfaced as the `universal_accounts` wasm-config flag. | `runtime/runtime/src/action_validation.rs:535` — `validate_universal_state_init`; `runtime/runtime/src/universal_account_id.rs:21` — `action_universal_state_init`; `runtime/runtime/src/actions.rs:918` — `get_account_type` |
| `EarlyKickout` | decl `:395`, map `:626` | Moved nightly (152) → stable 87. Chunk-producer assignment is precomputed, and early kickout is added. **Does not affect this component**; see [epoch, validators & staking](epoch-validators-staking.md). | — |

Two host-function families also stabilized at 87 without a `ProtocolFeature` variant — they are plain runtime-config flags flipped in `core/parameters/res/runtime_configs/87.yaml`: `sha3_host_fns` (SHA3-256/384/512, FIPS-202) and `ml_dsa_verify_host_fn` (ML-DSA-65, FIPS-204). See [contract VM](contract-vm.md). The same file also introduces `min_contract_size_per_local = 2` (`core/parameters/res/runtime_configs/87.yaml:11`) — the parameter is `Option<u64>` and was previously unset, so this is a new wasm-preparation limit (one local per two bytes of code), not a lowered one; see [contract VM](contract-vm.md).

### Still-live gates below 87

| Feature | Activates | Effect on this component |
| --- | --- | --- |
| `EnforcePerReceiptStorageProofLimit` | 86 (`version.rs:618`) | Snapshots the per-receipt storage-proof upper bound before a receipt's actions run (`runtime/runtime/src/lib.rs:925`); the snapshot is what `EnforceStorageProofLimitForAllActions` and the in-VM counter measure against (`runtime/runtime/src/ext.rs:325`). |
| `AccountCostIncrease` | 85 (`version.rs:617`) | `gas_burn_price = min(purchase, current)`, price-surplus refund, penalty priced at the burn price, `create_account_charge` (`runtime/runtime/src/lib.rs:1038`, `:1318`, `:1360`). |
| `ClampOutgoingGasAdmission` | 85 (`version.rs:617`) | Clamps outgoing-receipt gas admission in the receipt sink (`runtime/runtime/src/congestion_control.rs:443`). See [cross-shard congestion](cross-shard-congestion.md). |
| `GasKeys` | 85 (`version.rs:617`) | Gas-key transactions, the `DepositFailed` verdict, and the `AddKey`/`TransferToGasKey`/`WithdrawFromGasKey` gas-key forms (`runtime/runtime/src/action_validation.rs:213`, `runtime/runtime/src/verifier.rs:524`). |
| `DelegateV2` | 85 (`version.rs:617`) | Admits `Action::DelegateV2` — now immediately re-closed by `RejectDelegateV2` for new receipts (`runtime/runtime/src/action_validation.rs:186`). |
| `StrictNonce` | 85 (`version.rs:617`) | `NonceMode::Strict` is expressible; enforced in `verify_nonce` (`runtime/runtime/src/verifier.rs:266`). The bootstrap path forces Strict regardless (`runtime/runtime/src/verifier.rs:474`). |
| `PostQuantumSignatures` | 85 (`version.rs:617`) | ML-DSA-65 keys/signatures admitted; centrally gated for contract-emitted actions too (`runtime/runtime/src/action_validation.rs:108`). |
| `ExecutionMetadataV4` | 85 (`version.rs:617`) | Outcome carries the per-action `Vec<AccountContract>` (`runtime/runtime/src/lib.rs:1252`). |
| `UniqueChunkTransactions` | 85 (`version.rs:617`) | Duplicate tx hashes skipped within a chunk (`runtime/runtime/src/lib.rs:2110`). |
| `YieldWithId` | 85 (`version.rs:617`) | `promise_yield_create_with_id` / `promise_yield_resume_with_yield_id` host functions; see [contract VM](contract-vm.md). |
| `FixDeleteAccountGlobalContractStorageUsage` | 85 (`version.rs:617`) | `DeleteAccount` subtracts global-contract identifier storage instead of only local code (`runtime/runtime/src/actions.rs:342`). |
| `FixDelegateActionDepositWithFunctionCallError` | 85 (`version.rs:617`) | Early return on the `DepositWithFunctionCall` path in `validate_delegate_action_key` (`runtime/runtime/src/actions.rs:579`). |
| `FixDelegatedDeterministicStateInit` | 85 (`version.rs:617`) | Inner delegate actions are validated against the delegate's own `receiver_id`, which is what makes a delegated state init possible (`runtime/runtime/src/action_validation.rs:249`). |

### Deprecated since 2.13.0 — behavior is now unconditional

`MIN_SUPPORTED_PROTOCOL_VERSION` moved 83 → 84, so these are no longer branches in the code. Prose that used to describe a before/after now describes the only behavior:

| Former feature | Now |
| --- | --- |
| `InvalidTxGenerateOutcomes` (83) | Every failed transaction produces an outcome; `register_outcome` is gone (`runtime/runtime/src/lib.rs:2125`, `:2332`, `:2399`). |
| `FixAccessKeyAllowanceCharging` (83) | `check_and_compute_new_allowance` never mutates; the allowance travels only in `AccessKeyUpdate::Regular` (`runtime/runtime/src/verifier.rs:282`, `:365`). |
| `IncludeDeployGlobalContractOutcomeBurntStorage` (83) | Global-contract deploy burnt storage is always counted in the outcome (`runtime/runtime/src/global_contracts.rs:25`). |
| `GlobalContractDistributionNonce` (83) | Distribution receipts always carry and check a nonce (`runtime/runtime/src/global_contracts.rs:251` — `check_and_update_nonce`). |
| `EthImplicitGlobalContract` (83) | ETH implicit accounts are always created pointing at the wallet global contract (`runtime/runtime/src/actions.rs:255`). |
| `ExcludeExistingCodeFromWitnessForCodeLen` (83) | Code length is always read from the trie value-ref (`runtime/runtime/src/actions.rs:412` — `get_code_len_or_default`). |
| `InstantPromiseYield`, `YieldResumeImprovements`, `InstantDeleteAccount` (83) | Always on: yield status lives in the trie and single-`DeleteAccount` receipts are instant (`runtime/runtime/src/lib.rs:2748`). |
| `Wasmtime` (84) | Wasmtime is the only backend; the NearVM engine crates were deleted this release. See [contract VM](contract-vm.md). |
| `ReducedGasRefunds` / NEP-536 (78) | `refund_unspent_gas_and_deposits` always uses the reduced-refund model with the `gas_penalty_for_gas_refund` penalty (`runtime/runtime/src/lib.rs:1284`). |

`FixContractLoadingCost` remains **nightly (129)** (`version.rs:632`) and is distinct from the now-stable `FixContractLoadingError`.

### Ungated changes this release that alter execution

- **Runtime configs for protocol versions 46, 48, 49, 50 and 52 were deleted** (`core/parameters/res/runtime_configs/`), consistent with `MIN_SUPPORTED_PROTOCOL_VERSION = 84`. Replaying those heights needs an older binary; `clamp_to_supported_protocol_version` (`core/primitives-core/src/version.rs:662`) exists so read-only view calls can still be served, and `assert_supported_protocol_version` (`:672`) panics at callee boundaries otherwise.
- **VM errors panic again.** `VMRunnerError::Nondeterministic` and `WasmUnknownError` abort the node instead of being converted into outcomes (`runtime/runtime/src/function_call.rs:334`-`339`) — reverting the 2.13 softening.
- **Promise-yield index reads propagate storage errors** instead of defaulting: `get_promise_yield_indices(state_update)?` (`runtime/runtime/src/function_call.rs:153`).
- **Missing contract code on the witness path is a storage error, not a nop**: when the account commits to a code hash and the apply reason is `ValidateChunkStateWitness`, a missing body yields `StorageError::MissingTrieValue` (`runtime/runtime/src/function_call.rs:295`-`308`).
- **Gas-key host-function fees are priced on the decoded key length** (`pk_len = PublicKey::len()`, `core/crypto/src/signature.rs:267`) rather than the raw `public_key_len` argument the contract supplied (#16084; `runtime/near-vm-runner/src/wasmtime_runner/logic.rs:3549` — `promise_batch_action_transfer_to_gas_key`, `:3618` / `:3686` — `promise_batch_action_add_gas_key_with_full_access` / `..._with_function_call`). There is **no `ProtocolFeature` and no config flag** on this specific change; it is an unversioned fix that also shipped into the 2.13 release line (`e015669d`, contained in tags 2.13.1–2.13.4) before being cherry-picked to master, so every deployed 2.13.x binary already agrees with 2.14 at protocol 85/86. It is distinct from the version-gated `FixMlDsaCostCharging` above, which only chooses *which* length the exec fee uses. See **Open questions** for the divergence analysis.

## Invariants & failure modes

- **Gas ordering**: `merge` asserts `gas_burnt_for_function_call <= gas_burnt <= gas_used` per action (`runtime/runtime/src/lib.rs:488`).
- **Failed receipt atomicity**: a receipt whose result is `Err` triggers `state_update.rollback()`, so no state change persists except the outcome and gas accounting (`runtime/runtime/src/lib.rs:1086`). `set_error` additionally clears queued receipts, proposals and burnt/subsidized amounts (`runtime/runtime/src/lib.rs:535`).
- **A failed action stops the receipt**: the action loop breaks on the first `Err` and records the action index; subsequent actions never run (`runtime/runtime/src/lib.rs:1000`).
- **Staking invariant**: `update_validator_accounts` returns a fatal `StorageInconsistentState` if `locked < max_of_stakes`, if an account with positive stake is missing, or if an uninitialized account carries any stake/reward/proposal (`runtime/runtime/src/lib.rs:1730`, `:1762`, `:1796`).
- **Invalid txs make progress, not failure**: a chunk containing invalid transactions is not rejected; the offending txs are skipped during conversion, which pollutes the chain with junk but keeps the shard live (`runtime/runtime/src/lib.rs:1849` doc; skip sites `:2124`, `:2329`).
- **Refund receipts are free**: system-predecessor receipts burn zero gas and generate no refund; a failed refund burns its deposit into `other_burnt_amount` (`runtime/runtime/src/lib.rs:1090`, `:1050`).
- **Delayed receipts must stay valid**: a delayed receipt failing `validate_receipt` on dequeue is `StorageInconsistentState` (`runtime/runtime/src/lib.rs:2628`); an invalid *incoming* receipt is a `RuntimeError::ReceiptValidationError` and is never stored as delayed (`runtime/runtime/src/lib.rs:2696`).
- **Verification never mutates**: all three `verify_and_charge_*_ephemeral` functions are pure; `VerificationResult::apply` is the only mutation point and reports a mismatch as `StorageInconsistentState` rather than panicking, because it runs during chunk apply (`runtime/runtime/src/lib.rs:337`).
- **Pending constraints are consensus-neutral**: `PendingConstraints` is always default on the consensus path, and every use is a `saturating_sub` so the RPC / chunk-production path cannot change the result (`runtime/runtime/src/verifier.rs:353`, `:644`).
- **Storage staking**: the receiver must cover storage after execution or the receipt fails with `LackBalanceForState` (`runtime/runtime/src/lib.rs:1015`); zero-balance accounts (≤ 770 bytes) are exempt (`runtime/runtime/src/verifier.rs:25`, `:88`).
- **Uninitialized accounts are inert**: every action except `UniversalStateInit`, `DeterministicStateInit` and `Transfer` fails with `ActionErrorKind::AccountNotInitialized` (`runtime/runtime/src/actions.rs:884`), and `Account::initialize` refuses a second call (`core/primitives-core/src/account.rs:330`).
- **Bootstrap nonces do not replay**: the bootstrap nonce is consumed on the account before the state init runs, and installed keys start at `max(initial_nonce_value, consumed nonce)` (`runtime/runtime/src/lib.rs:371`, `runtime/runtime/src/universal_account_id.rs:66`).
- **Chunk work is bounded** by `compute_limit` (= gas limit) and the chunk storage-proof size limit; overflow pushes work onto the delayed queue (`runtime/runtime/src/lib.rs:2514`, `:2595`, `:2704`).
- **`CreateAccount` never targets an implicit id** — it would claim `actor_id` for an id whose private key the sender may not hold; `ActionErrorKind::OnlyImplicitAccountCreationAllowed` (`runtime/runtime/src/actions.rs:836`).

## Code anchors

| Location | Symbol | What happens here |
| --- | --- | --- |
| `runtime/runtime/src/lib.rs:1860` | `Runtime::apply` | Top-level chunk state transition; ordered phases. |
| `runtime/runtime/src/lib.rs:1917` | `apply` missing-chunk branch | Short-circuit for missing chunks → `missing_chunk_apply_result` `:3064`. |
| `runtime/runtime/src/lib.rs:1717` | `update_validator_accounts` | Phase 1: rewards / return stake / treasury. |
| `runtime/runtime/src/lib.rs:2011` | `process_transactions` | Phase 2: parallel validate + prefetch, then charge and convert. |
| `runtime/runtime/src/lib.rs:2787` | `process_receipts` | Phase 3: local → delayed → incoming, then yield timeouts. |
| `runtime/runtime/src/lib.rs:2486` | `process_local_receipts` | Drains local receipts; overflow → delayed. |
| `runtime/runtime/src/lib.rs:2570` | `process_delayed_receipts` | Drains the delayed backlog; re-validates on dequeue. |
| `runtime/runtime/src/lib.rs:2670` | `process_incoming_receipts` | Validates then executes or delays incoming receipts. |
| `runtime/runtime/src/lib.rs:2748` | `process_receipt_and_instant_receipts` | Drains instant receipts right after their parent. |
| `runtime/runtime/src/lib.rs:1421` | `process_receipt` | Dispatch by receipt kind (data / action / yield / resume / global-contract distribution). |
| `runtime/runtime/src/lib.rs:827` | `apply_action_receipt` | Execute one action receipt end-to-end. |
| `runtime/runtime/src/lib.rs:848` | promise-input size check | `ReceiptPromiseInputSizeLimit` / `TotalPromiseInputSizeExceeded`. |
| `runtime/runtime/src/lib.rs:982` | per-receipt storage-proof check | `EnforceStorageProofLimitForAllActions` / `ReceiptStorageProofSizeExceeded`. |
| `runtime/runtime/src/lib.rs:571` | `apply_action` | Per-action fee seeding, existence/permission checks, dispatch. |
| `runtime/runtime/src/lib.rs:1284` | `refund_unspent_gas_and_deposits` | Gas + deposit refunds, NEP-536 penalty, `create_account_charge`. |
| `runtime/runtime/src/lib.rs:2852` | `validate_apply_state_update` | Phase 4: finalize state, congestion, bandwidth, proposal dedup. |
| `runtime/runtime/src/lib.rs:3016` | `action_transfer_or_implicit_account_creation` | Transfer, gas-refund routing, implicit account creation. |
| `runtime/runtime/src/lib.rs:3113` | `resolve_promise_yield_timeouts` | Times out yielded receipts into `PromiseResume` receipts. |
| `runtime/runtime/src/config.rs:464` | `tx_cost` / `calculate_tx_cost` `:481` | Send fees burnt vs. attached prepaid gas; receipt gas price. |
| `runtime/runtime/src/config.rs:334` | `exec_fee` | Per-action execution fee (gas + compute). |
| `runtime/runtime/src/config.rs:86` | `total_send_fees` | Per-action send fees (sir-aware). |
| `runtime/runtime/src/config.rs:73` | `gas_key_send_pk_len` | `FixMlDsaCostCharging`: send fee on `len()`, exec on `trie_id_len()`. |
| `runtime/runtime/src/config.rs:589` | `signature_verification_cost` | Tx signature cost; inner-delegate compute moved to the receiver shard. |
| `runtime/runtime/src/config.rs:242` | `universal_state_init_fee` | Fee terms for `UniversalStateInit`, shared with the VM host function. |
| `runtime/runtime/src/verifier.rs:109` | `validate_transaction` | Stateless validation: actions + `ValidatedTransaction::new`. |
| `runtime/runtime/src/verifier.rs:164` | `get_signer_and_authorization` | Resolves `TxAuthorization` (access key / gas key / self-signed state init). |
| `runtime/runtime/src/verifier.rs:312` | `verify_and_charge_tx_ephemeral` | Regular-tx nonce/balance/allowance/storage/permission checks. |
| `runtime/runtime/src/verifier.rs:424` | `verify_and_charge_bootstrap_tx_ephemeral` | Self-signed universal state init; forces `Strict` nonce. |
| `runtime/runtime/src/verifier.rs:524` | `verify_and_charge_gas_key_tx_ephemeral` | Gas-key charge path; `DepositFailed`. |
| `runtime/runtime/src/verifier.rs:254` | `verify_nonce` | Monotonic vs. strict nonce, plus the height upper bound. |
| `runtime/runtime/src/verifier.rs:681` | `validate_receipt` | Receipt size/id/action validation (`NewReceipt` vs. `ExistingReceipt`). |
| `runtime/runtime/src/action_validation.rs:86` | `validate_actions_with_mode` | Action-count, post-quantum, deploy/state-init caps, delegate rules, prepaid-gas cap. |
| `runtime/runtime/src/action_validation.rs:45` | `validate_number_of_state_init_entries` | `max_state_init_entries` (1 500) over the whole receipt. |
| `runtime/runtime/src/action_validation.rs:311` | `validate_function_call_action` | Zero-gas and `RejectEmptyMethodName` checks. |
| `runtime/runtime/src/action_validation.rs:222` | `validate_delegate_action` | Nested-action cap, `RejectWithdrawFromGasKeyInDelegate`, inner receiver. |
| `runtime/runtime/src/action_validation.rs:535` | `validate_universal_state_init` | `0u` id derivation, payload decoding, key and storage caps. |
| `runtime/runtime/src/actions.rs:814` | `check_account_existence` | Per-action account existence and `AccountNotInitialized` rules. |
| `runtime/runtime/src/actions.rs:929` | `implicit_creation_allowed` | Which implicit ids a transfer may create, and when. |
| `runtime/runtime/src/actions.rs:899` | `get_account_type` | Config-aware account-type resolution (no implicit protocol upgrade). |
| `runtime/runtime/src/actions.rs:755` | `check_actor_permissions` | actor == account checks; zero-stake requirement for delete. |
| `runtime/runtime/src/actions.rs:58` | `action_stake` | Stake proposal + amount↔locked move. |
| `runtime/runtime/src/actions.rs:330` | `action_delete_account` | Beneficiary refund, gas-key balance burn, storage/compute accounting. |
| `runtime/runtime/src/actions.rs:453` | `apply_delegate_action` | Meta-transaction verify (compute metered here) + inner receipt spawn. |
| `runtime/runtime/src/actions.rs:579` | `validate_delegate_action_key` | Inner key lookup, nonce advance, permission checks. |
| `runtime/runtime/src/function_call.rs:31` | `action_function_call` | VM invocation, yield-timeout enqueue, receipt construction. |
| `runtime/runtime/src/function_call.rs:233` | `execute_function_call` | `VMContext` assembly and `VMRunnerError` triage (panics on nondeterminism). |
| `runtime/runtime/src/global_contracts.rs:191` | `apply_distribution_current_shard` | Global-contract install + `GlobalContractSameChunkCallFix` deploy record. |
| `runtime/runtime/src/deterministic_account_id.rs:73` | `settle_state_init_deposit` | Shared deposit-vs-storage-stake settlement for both state-init actions. |
| `runtime/runtime/src/universal_account_id.rs:21` | `action_universal_state_init` | `0u` account install and deposit settlement. |
| `runtime/runtime/src/universal_account_id.rs:102` | `install_universal_account` | Code, storage entries, keys, storage-usage accounting. |
| `core/primitives/src/utils.rs:502` | `derive_universal_account_id` | SHA3-256 over the raw state-init bytes → `0u` id. |
| `core/primitives/src/transaction.rs:236` | `Transaction::state_init_bootstrap` | Shape test for the self-signed bootstrap transaction. |
| `core/parameters/res/runtime_configs/87.yaml` | v87 parameter diff | `burnt_gas_reward` → 0, `max_receipt_total_input_size`, `max_state_init_entries`, `fix_*` and `universal_accounts` flags, SHA3/ML-DSA host-fn flags. |
| `core/primitives-core/src/version.rs:500` | `ProtocolFeature::protocol_version` | Feature → activation version mapping. |

## Open questions

- **RESOLVED — `fix(gas-keys): fix gas charging issue` (#16084) is deliberately unversioned, and also shipped into the 2.13 line.** The commit (`b6612ed6` on this branch) changes the gas-key host functions from pricing on the caller-supplied `public_key_len` to pricing on the decoded key's wire length `pk_len` (`runtime/near-vm-runner/src/wasmtime_runner/logic.rs:3549`, `:3618`, `:3686`). There is no `ProtocolFeature` variant and no `87.yaml` flag guarding it — `fix_ml_dsa_cost_charging` (`ProtocolFeature::FixMlDsaCostCharging`, added later by #16064) gates only *which* length the **exec** fee uses (`gas_key_exec_pk_len`, `runtime/near-vm-runner/src/logic/logic.rs:194`); the **send** fee reads `pk_len` unconditionally. The reachable behavioral surface is narrow:
  - **Memory-read calls whose key decodes** (the normal case) are unaffected: borsh `try_from_slice` rejects trailing bytes, so a successful decode implies `pk_len == public_key_len`.
  - **Register-convention calls** (`public_key_len == u64::MAX`, `get_memory_or_register`, `runtime/near-vm-runner/src/wasmtime_runner/logic.rs:145`) were a **node panic**, not a consensus outcome: the old code fed `usize::MAX` into `checked_mul(..).unwrap()` in `gas_key_transfer_send_fee` / `gas_key_add_key_exec_fee` (`core/parameters/src/cost.rs:865` — `gas_key_transfer_send_fee`, `:928` — `gas_key_add_key_exec_fee`). A panic is not a state any chain advanced through, so there is nothing to preserve.
  - **Keys that fail to decode**: the old code burnt `gas_key_byte` × `public_key_len` before returning `HostError::InvalidPublicKey`; the new code burns zero for those bytes and then returns the same error. Because a failed function call records `result.gas_burnt = outcome.burnt_gas` rather than the whole prepaid amount (`runtime/runtime/src/function_call.rs:140`), this *is* consensus-visible.
  So the change is a genuine unversioned consensus change at protocol 85/86 (gas-key host functions have been live since `gas_key_host_fns` in `core/parameters/res/runtime_configs/85.yaml:3`), and it was handled as a patch release rather than a protocol feature: `e015669d` sits directly on the `2.13-release` branch and is contained in tags 2.13.1, 2.13.2, 2.13.3 and 2.13.4, with `b6612ed6` the cherry-pick to master. The only binaries that disagree with 2.14 are 2.13.0 and its release candidates, which are superseded. **No open divergence remains between currently deployed binaries**; what remains unverifiable from code alone is whether a NEP/release note documents the unversioned fix.

- The exact interaction between a **`0u` account created by a batched transfer** and a `UniversalStateInit` *later in the same receipt* that fails is rolled back wholesale by `apply_action_receipt`; whether an uninitialized-but-funded account can survive a chunk when the transfer and the state init arrive in *different* receipts is by design (the comment at `runtime/runtime/src/universal_account_id.rs:38` says a half-installed account cannot be observed), but no production code asserts the cross-receipt case.
- `validate_apply_state_update` reads `processing_state.outcomes` and `processing_state.metrics` after `processing_state` is destructured with `..` (`runtime/runtime/src/lib.rs:2852`, `:2983`). This compiles as a partial move and is behaviorally fine, but the mixed style makes the field ownership hard to follow; flagged for a future reader rather than as a defect.
