[package]
name = "style-tests"
version = "0.1.0"
edition = "2021"
publish = false
description = "An entry point for miscelaneous (stylistic) workspace-wide tests"

[dependencies]
tempfile.workspace = true
