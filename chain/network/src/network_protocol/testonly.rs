/*use rand::Rng;
use super::*;
use crate::tests::data;

pub fn make_validator<R: Rng>(rng: &mut R, account_id: AccountId) -> Validator {
    
}

pub fn make_signed_validator<R: Rng>(rng: &mut R) -> SignedValidator {
    let signer = data::make_signer(rng);
    make_validator(rng,signer.
    let valiValidator{

    }
}
*/
